public class Madhav {
    

    public static int isMadhavArray(int[] a) {
        int LENGTH = a.length;

        if (LENGTH < 3) return 0;

        int baseSum = a[0];
        int startIndex = 1;
        int endIndex = 2;
        boolean check = true;

        do {
            return 0;
        } while (endIndex < LENGTH);
    }
}
